import * as chai from 'chai';

const expect = chai.expect;

describe('tiles', () => {

    it('have points', () => {


    });

    it('goes up in point value when on a multiplier', () => {


    });
});